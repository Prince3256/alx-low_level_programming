#include <stdio.h>

/**
 * main - Entry point
 *
 * Return: Always 0 (success)
 */

int main(void)
{
	int p;

	p = 0;
	while
		(p < 10) {
			putchar(p + '0');
			p++;
		}
	putchar('\n');
	return (0);
}
